language: cpp

matrix:
  include:
    - os: linux
      dist: trusty
      compiler: gcc
      addons:
       apt:
         packages:
           - cmake
           - sqlite3
           - g++
           - python-pytest

     
script:
     - SQLITE_HOME=$(which sqlite3)
     - ls -l $SQLITE_HOME
     - export CMAKE_PREFIX_PATH=$CMAKE_PREFIX_PATH:$SQLITE_HOME
     - mkdir -p build
     - cd build
     - cmake -D-DCMAKE_INSTALL_PREFIX=./../ -DSQLite_DIR="/usr/local/opt/sqlite"  ..
     - make
     - make install
     - cd ..
     - export PATH=$PATH:./bin
     - py.test
